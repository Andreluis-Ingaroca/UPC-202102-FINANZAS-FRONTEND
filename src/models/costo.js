export default class Costo {
    constructor(id, nombre,costoInicial, porcentaje, monto) {
        this.id = id;
        this.nombre=nombre;
        this.costoInicial = costoInicial;
        this.porcentaje = porcentaje;
        this.monto = monto;

    }
}